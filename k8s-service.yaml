apiVersion: v1
kind: Service
metadata:
  name: llm-smeta-pir
  labels:
    app: llm-smeta-pir
spec:
  type: ClusterIP
  ports:
  - port: 8010
    targetPort: 8010
    protocol: TCP
    name: http
  selector:
    app: llm-smeta-pir
---
# Optional: Ingress for external access
# Uncomment and configure if you need external access
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: llm-smeta-pir-ingress
#   annotations:
#     nginx.ingress.kubernetes.io/rewrite-target: /
#     # Add other ingress annotations as needed
# spec:
#   ingressClassName: nginx
#   rules:
#   - host: llm-smeta-pir.yourdomain.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: llm-smeta-pir
#             port:
#               number: 8010
